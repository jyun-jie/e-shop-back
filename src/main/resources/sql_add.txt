



/*
在訂單中的商品

CREATE TABLE `e_shop`.`inorderproduct` (
  `order_contentId` INT NOT NULL AUTO_INCREMENT,
  `orderId` INT NULL,
  `product_Id` INT NULL,
  `productName` VARCHAR(40) NULL,
  `price` DOUBLE NULL,
  `quantity` INT NULL,
  `InOrderProductcol` VARCHAR(45) NULL,
  `create_Time` DATETIME NULL,
  `update_time` DATETIME NULL,
  PRIMARY KEY (`order_contentId`));

 */

/*
訂單的商品
create table `e_shop`.`order` (
   `id` INT NOT NULL AUTO_INCREMENT,
   `userId` INT NULL,
   `sellerId` INT NULL,
   `state` VARCHAR(50) NULL,
   `create_Time` DATETIME NULL,
   `completed_at` DATETIME NULL,
   `total` DOUBLE NULL,
   `postalAddress` VARCHAR(50) NULL,
   `receiverAddress` VARCHAR(50) NULL,
   `postalName` VARCHAR(50) NULL,
   `receiverName` VARCHAR(50) NULL,
   `payment_method` VARCHAR(50) NULL,
   `isPay` TINYINT NULL
   PRIMARY KEY (`id`));


*/